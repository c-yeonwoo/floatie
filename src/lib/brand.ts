/** Product brand — KR display / EN store & mascot */
export const BRAND_KO = "플로티";
export const BRAND_EN = "Floatie";

export function pageTitle(section: string): string {
  return `${section} — ${BRAND_KO}`;
}
